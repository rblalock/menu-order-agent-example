// Use a generic placeholder for all images for now
const PLACEHOLDER_IMAGE = "https://via.placeholder.com/400x300/f3f4f6/9ca3af?text=Menu+Item";

export function getItemImage(itemName: string, category?: string): string {
  // Return placeholder for all items for now
  return PLACEHOLDER_IMAGE;
}